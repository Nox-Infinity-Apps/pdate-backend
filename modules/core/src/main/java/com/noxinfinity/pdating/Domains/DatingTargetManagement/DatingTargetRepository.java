package com.noxinfinity.pdating.Domains.DatingTargetManagement;

import com.noxinfinity.pdating.Entities.DatingTarget;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DatingTargetRepository extends JpaRepository<DatingTarget, Long> {
}
