extend type Mutation {
    addToTestUser(name: String!): Response
    login(loginRequest: LoginRequest!): ResponseLogin
}

type ResponseLogin {
    statusCode: Int!
    message: String!
    token: String
}

input LoginRequest {
    username: String!
    password: String!
}

type Response{
    statusCode: Int!
    statusMessage: String!
}
