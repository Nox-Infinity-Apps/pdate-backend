extend type Mutation {
    addToTestUser(name: String!): Response
    login(loginRequest: LoginRequest!): Response_Login
}

type Response_Login {
    statusCode: Int!
    token: String!
}

input LoginRequest {
    username: String!
    password: String!
}

type Response{
    statusCode: Int!
    statusMessage: String!
}
